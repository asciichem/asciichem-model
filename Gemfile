# frozen_string_literal: true

# Maintainer tooling only — this repository is the implementation-
# independent contract (lutaml models, schemas, generated types) and
# is NOT distributed as a rubygem. The type generator needs
# lutaml-model; the example validator needs json_schemer.
source "https://rubygems.org"

gem "json_schemer", "~> 2.4"
gem "lutaml-model", ">= 0.8", "< 2"
gem "rake"
